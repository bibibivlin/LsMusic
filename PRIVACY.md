# 隐私说明

生效日期：2026-09-03

L's Music 是一个开源 Android DLNA 控制器，不包含广告、用户画像、遥测或第三方崩溃分析 SDK。应用仅在提供用户主动启用的功能时处理网络和设备数据。

## 局域网功能

应用使用 SSDP / UPnP 在当前局域网发现媒体服务器和播放设备，并向用户选择的设备发送浏览与播放控制请求。设备名称、标识和连接地址会在设备上用于恢复选择。音乐通常由播放设备直接从媒体服务器获取，手机不会代为转发完整音频流；选择“本机”播放时，手机会直接访问媒体服务器提供的音频地址。

## 在线歌词

在线歌词默认关闭。启用后，应用会将匹配歌词所需的曲名、艺术家、专辑和时长发送给用户配置顺序中的网易云音乐和 QQ 音乐公开接口。应用不登录这些服务，不保存账户 Cookie，也不使用第三方代理。成功解析的歌词和短期未找到结果会保存在应用缓存目录，用户可在设置中清除；关闭在线歌词后不会读取缓存或请求服务。

## ListenBrainz

ListenBrainz 上报默认关闭。用户提供并验证令牌、再主动启用后，应用会向 ListenBrainz 发送令牌、曲目元数据、播放开始时间和满足门槛的播放记录。令牌保存在 Android 应用私有偏好文件中，并排除在云备份和设备迁移之外。尚未成功上传的正式播放记录保存在应用私有存储中，成功后删除；用户可以重试或删除这些记录。

## 封面与音频标签

为显示高清封面或完善 ListenBrainz 元数据，应用可能向用户的媒体服务器发起有上限的 HTTP 分段请求，读取音频文件中的标签或图片区段。应用不会为此下载完整音频文件。解析后的封面只保存在有大小限制的临时缓存中。

## 系统权限

- 局域网和 Wi-Fi 权限用于发现与控制 DLNA / UPnP 设备。
- 网络权限用于媒体访问、在线歌词和用户主动启用的 ListenBrainz 功能。
- 通知、前台服务和唤醒锁用于持续播放及 Android 系统媒体控制。
- 开机完成权限仅用于在满足用户配置时恢复待上传 ListenBrainz 记录的重试调度。

## 数据控制

用户可以关闭在线歌词与 ListenBrainz、清除歌词缓存、删除待上传记录，或通过卸载应用删除应用私有数据。第三方服务对已经接收的数据适用其各自的隐私政策。

如对本说明或数据处理有疑问，可在不包含敏感信息的前提下创建 GitHub Issue；安全或隐私漏洞请按 [安全政策](SECURITY.md) 私下报告。
